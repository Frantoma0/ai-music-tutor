diff --git a/backend/app/api/uploads.py b/backend/app/api/uploads.py
index d023539..98864af 100644
--- a/backend/app/api/uploads.py
+++ b/backend/app/api/uploads.py
@@ -1,3 +1,4 @@
+import sys
 import shutil
 import subprocess
 from typing import Any
@@ -21,7 +22,9 @@ def safe_name(value: str) -> str:
 
 def read_youtube_metadata(url: str) -> dict[str, str | None]:
     metadata_command = [
-        "/usr/local/bin/yt-dlp",
+        sys.executable,
+        "-m",
+        "yt_dlp",
         "--js-runtimes",
         "deno",
         "--skip-download",
@@ -122,7 +125,9 @@ async def upload_youtube_audio(payload: dict[str, Any]):
     output_template = raw_dir / "downloaded.%(ext)s"
 
     download_command = [
-        "/usr/local/bin/yt-dlp",
+        sys.executable,
+        "-m",
+        "yt_dlp",
         "--js-runtimes",
         "deno",
         "-f",
diff --git a/backend/app/mcp_tools/tools.py b/backend/app/mcp_tools/tools.py
index 7516270..eff26da 100644
--- a/backend/app/mcp_tools/tools.py
+++ b/backend/app/mcp_tools/tools.py
@@ -400,6 +400,26 @@ class RunAudioToAnalysisTool(MCPTool):
                         "description": "Skip Demucs source separation and transcribe the normalized WAV directly.",
                         "default": False,
                     },
+                    "preprocess_audio": {
+                        "type": "boolean",
+                        "description": "Apply safe audio preprocessing before transcription.",
+                        "default": True,
+                    },
+                    "trim_silence": {
+                        "type": "boolean",
+                        "description": "Trim leading silence before transcription.",
+                        "default": True,
+                    },
+                    "normalize_audio": {
+                        "type": "boolean",
+                        "description": "Normalize input volume before transcription.",
+                        "default": True,
+                    },
+                    "highpass_filter": {
+                        "type": "boolean",
+                        "description": "Apply a low rumble high-pass filter before transcription.",
+                        "default": True,
+                    },
                     "persist": {
                         "type": "boolean",
                         "description": "Persist the completed pipeline result to SQLite.",
@@ -426,6 +446,7 @@ class RunAudioToAnalysisTool(MCPTool):
                     "extract": {"type": "object"},
                     "separation": {"type": "object"},
                     "separation_quality": {"type": "object"},
+                    "preprocessing": {"type": "object"},
                     "transcription": {"type": "object"},
                     "analysis": {"type": "object"},
                     "final_audio_path": {"type": ["string", "null"]},
@@ -441,6 +462,7 @@ class RunAudioToAnalysisTool(MCPTool):
                     "extract",
                     "separation",
                     "separation_quality",
+                    "preprocessing",
                     "transcription",
                     "analysis",
                 ],
@@ -471,6 +493,10 @@ class RunAudioToAnalysisTool(MCPTool):
             use_basic_pitch=bool(payload.get("use_basic_pitch", True)),
             selected_stem=payload.get("selected_stem", "other"),
             skip_separation=bool(payload.get("skip_separation", False)),
+            preprocess_audio=bool(payload.get("preprocess_audio", True)),
+            trim_silence=bool(payload.get("trim_silence", True)),
+            normalize_audio=bool(payload.get("normalize_audio", True)),
+            highpass_filter=bool(payload.get("highpass_filter", True)),
         )
 
         data = result.to_dict()
diff --git a/backend/app/pipeline/orchestrator.py b/backend/app/pipeline/orchestrator.py
index b674657..05c7177 100644
--- a/backend/app/pipeline/orchestrator.py
+++ b/backend/app/pipeline/orchestrator.py
@@ -12,6 +12,7 @@ from app.pipeline.separation_quality import analyze_separation_quality
 from app.pipeline.resource_guard import gpu_sequential_guard
 from app.pipeline.source_separation import SourceSeparationResult, separate_sources
 from app.pipeline.tracer import run_tracer_bullet
+from app.pipeline.audio_preprocess import preprocess_audio_for_transcription
 
 
 @dataclass
@@ -22,6 +23,7 @@ class AudioToAnalysisPipelineResult:
     extract: dict[str, Any]
     separation: dict[str, Any]
     separation_quality: dict[str, Any]
+    preprocessing: dict[str, Any]
     transcription: dict[str, Any]
     analysis: dict[str, Any]
     final_audio_path: str | None
@@ -75,6 +77,13 @@ def _load_transcription_metadata(
 def _empty_quality() -> dict[str, Any]:
     return {}
 
+def _empty_preprocessing() -> dict[str, Any]:
+    return {
+        "status": "skipped",
+        "enabled": False,
+        "filters": [],
+        "error": None,
+    }
 
 def _analyze_separation_quality_safely(
     extract_result: AudioExtractionResult,
@@ -133,6 +142,10 @@ def run_audio_to_analysis_pipeline(
     use_basic_pitch: bool = True,
     selected_stem: str = "other",
     skip_separation: bool = False,
+    preprocess_audio: bool = True,
+    trim_silence: bool = True,
+    normalize_audio: bool = True,
+    highpass_filter: bool = True,
 ) -> AudioToAnalysisPipelineResult:
     """
     Day 6 orchestrator:
@@ -152,6 +165,7 @@ def run_audio_to_analysis_pipeline(
     empty_separation: dict[str, Any] = {}
     empty_transcription: dict[str, Any] = {}
     empty_analysis: dict[str, Any] = {}
+    empty_preprocessing: dict[str, Any] = _empty_preprocessing()
 
     try:
         extract_result: AudioExtractionResult = extract_audio(
@@ -170,6 +184,7 @@ def run_audio_to_analysis_pipeline(
                 extract=extract_data,
                 separation=empty_separation,
                 separation_quality=_empty_quality(),
+                preprocessing=empty_preprocessing,
                 transcription=empty_transcription,
                 analysis=empty_analysis,
                 final_audio_path=None,
@@ -222,6 +237,7 @@ def run_audio_to_analysis_pipeline(
                     extract=extract_data,
                     separation=separation_data,
                     separation_quality=_empty_quality(),
+                    preprocessing=empty_preprocessing,
                     transcription=empty_transcription,
                     analysis=empty_analysis,
                     final_audio_path=None,
@@ -241,6 +257,31 @@ def run_audio_to_analysis_pipeline(
                 separation_quality_data=separation_quality_data,
             )
 
+        preprocessing_data = _empty_preprocessing()
+
+        if preprocess_audio:
+            preprocessed_audio_path = Path(processed_dir) / job_id / "preprocessed.wav"
+
+            preprocess_result = preprocess_audio_for_transcription(
+                transcription_audio_path,
+                preprocessed_audio_path,
+                trim_silence=trim_silence,
+                normalize_audio=normalize_audio,
+                highpass_filter=highpass_filter,
+            )
+
+            preprocessing_data = {
+                "status": preprocess_result.status,
+                "input_path": preprocess_result.input_path,
+                "output_path": preprocess_result.output_path,
+                "enabled": preprocess_result.enabled,
+                "filters": preprocess_result.filters,
+                "error": preprocess_result.error,
+            }
+
+            if preprocess_result.status == "completed":
+                transcription_audio_path = str(preprocessed_audio_path)    
+
         tracer_result: TracerBulletResult = run_tracer_bullet(
             audio_path=transcription_audio_path,
             artifacts_dir=artifacts_dir,
@@ -264,6 +305,7 @@ def run_audio_to_analysis_pipeline(
                 extract=extract_data,
                 separation=separation_data,
                 separation_quality=separation_quality_data,
+                preprocessing=preprocessing_data,
                 transcription=transcription_data,
                 analysis=analysis_data,
                 final_audio_path=transcription_audio_path,
@@ -280,6 +322,7 @@ def run_audio_to_analysis_pipeline(
             extract=extract_data,
             separation=separation_data,
             separation_quality=separation_quality_data,
+            preprocessing=preprocessing_data,
             transcription=transcription_data,
             analysis=analysis_data,
             final_audio_path=transcription_audio_path,
@@ -297,6 +340,7 @@ def run_audio_to_analysis_pipeline(
             extract=empty_extract,
             separation=empty_separation,
             separation_quality=_empty_quality(),
+            preprocessing=empty_preprocessing,
             transcription=_empty_transcription(),
             analysis=empty_analysis,
             final_audio_path=None,
diff --git a/backend/tests/test_mcp_audio_to_analysis_tool.py b/backend/tests/test_mcp_audio_to_analysis_tool.py
index a28a8cb..315f2fb 100644
--- a/backend/tests/test_mcp_audio_to_analysis_tool.py
+++ b/backend/tests/test_mcp_audio_to_analysis_tool.py
@@ -44,6 +44,10 @@ def test_run_audio_to_analysis_tool_via_api(monkeypatch):
         use_basic_pitch=True,
         selected_stem="other",
         skip_separation=False,
+        preprocess_audio=True,
+        trim_silence=True,
+        normalize_audio=True,
+        highpass_filter=True,
         **kwargs,
     ):
         return AudioToAnalysisPipelineResult(
@@ -61,6 +65,12 @@ def test_run_audio_to_analysis_tool_via_api(monkeypatch):
                 "decision": "use_selected_stem",
                 "likely_solo_piano": False,
             },
+            preprocessing={
+                "status": "skipped",
+                "enabled": False,
+                "filters": [],
+                "error": None,
+            },
             transcription={
                 "status": "completed",
                 "transcription_method": "basic_pitch",
@@ -130,6 +140,12 @@ def test_run_audio_to_analysis_tool_can_persist_result(tmp_path, monkeypatch):
             extract={"status": "completed"},
             separation={"status": "completed"},
             separation_quality={"status": "completed"},
+            preprocessing={
+                "status": "skipped",
+                "enabled": False,
+                "filters": [],
+                "error": None,
+            },
             transcription={
                 "status": "completed",
                 "input_audio": "data/processed/pytest/input.wav",
diff --git a/frontend/src/main.jsx b/frontend/src/main.jsx
index 95ca565..cea35ef 100644
--- a/frontend/src/main.jsx
+++ b/frontend/src/main.jsx
@@ -269,6 +269,7 @@ function App() {
   const [newLessonStatus, setNewLessonStatus] = useState("idle");
   const [newLessonError, setNewLessonError] = useState("");
   const [newLessonStep, setNewLessonStep] = useState("");
+  const [useSourceSeparation, setUseSourceSeparation] = React.useState(false);
 
   const lessonMeta = lesson?.meta ?? {};
   const lessonDurationSeconds =
@@ -840,7 +841,8 @@ async function handleCreateNewLesson() {
       stems_dir: "data/stems",
       artifacts_dir: "artifacts/tracer",
       use_basic_pitch: true,
-      skip_separation: true,
+      skip_separation: !useSourceSeparation,
+      selected_stem: "other",
       preprocess_audio: isAnyAudioPreprocessingEnabled,
       trim_silence: audioPreprocessOptions.trim_silence,
       normalize_audio: audioPreprocessOptions.normalize_audio,
@@ -1436,6 +1438,23 @@ return (
               )}
             </div>
 
+            <div className="new-lesson-source-panel">
+              <label className="source-separation-toggle">
+                <input
+                  type="checkbox"
+                  checked={useSourceSeparation}
+                  onChange={() => setUseSourceSeparation((currentValue) => !currentValue)}
+                />
+
+                <span>
+                  <strong>Use source separation</strong>
+                  <small>
+                    Slower, but useful for songs with vocals or full instrumental mixes.
+                  </small>
+                </span>
+              </label>
+            </div>
+
             {newLessonStatus === "running" && (
               <div className="new-lesson-processing-panel">
                 <div className="new-lesson-processing-spinner" aria-hidden="true" />
diff --git a/frontend/src/styles.css b/frontend/src/styles.css
index ba3edd5..f5dc182 100644
--- a/frontend/src/styles.css
+++ b/frontend/src/styles.css
@@ -7314,3 +7314,46 @@ main.app-shell {
   transform: translateY(2px) rotate(225deg);
 }
 
+.new-lesson-source-panel {
+  padding: 10px 12px;
+  border-radius: 18px;
+  border: 1px solid rgba(127, 99, 199, 0.14);
+  background:
+    linear-gradient(
+      135deg,
+      rgba(255, 255, 255, 0.88),
+      rgba(245, 240, 255, 0.62)
+    );
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.82);
+}
+
+.source-separation-toggle {
+  display: grid;
+  grid-template-columns: auto 1fr;
+  gap: 10px;
+  align-items: center;
+  cursor: pointer;
+}
+
+.source-separation-toggle input {
+  accent-color: #8b64dc;
+}
+
+.source-separation-toggle span {
+  display: grid;
+  gap: 2px;
+  min-width: 0;
+}
+
+.source-separation-toggle strong {
+  color: #33285c;
+  font-size: 0.92rem;
+  line-height: 1.1;
+}
+
+.source-separation-toggle small {
+  color: #7a7199;
+  font-size: 0.74rem;
+  font-weight: 700;
+  line-height: 1.25;
+}
